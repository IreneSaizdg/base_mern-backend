# ---------------------------------------------
# Archivo de ejemplo de variables de entorno
# para un backend MERN (Node.js + Express + MongoDB)
# Copia este archivo como .env y reemplaza los valores.
# ---------------------------------------------

# Puerto del servidor
PORT = 3000

# Conexión a MongoDB (reemplaza con tu URI real)
MONGO_URI = uri_de_conexion_con_mongo


# SIN USAR ------------------------------------
# Clave secreta para firmar y verificar JWT
JWT_SECRET_KEY=your_jwt_secret_key_here

# Duración del token JWT (ejemplo: 1d = 1 día)
JWT_EXPIRES_IN=1d

# URL permitida desde el frontend (CORS)
CLIENT_URL=http://localhost:5173

# Modo de entorno (development o production)
NODE_ENV=development
